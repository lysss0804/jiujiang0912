# 「九江」银行外包供应商风险 Multi-Agent — 全链路演示 & 测试报告

> **作者**：成员 B · 梁雨珊（Algorithm / Multi-Agent 侧）
> **用途**：智能体交接验收凭证（供后端接手方核查）
> **演示时间**：2026-09-11
> **项目路径**：`jiujiang/`
> **演示性质**：**真实全链路 + 全功能实测**（非模板兜底、非 Mock）

---

## 一、一分钟结论（给验收人）

| 验收项 | 结论 | 证据位置 |
| --- | --- | --- |
| 真实大模型链路 | ✅ 调用 **GLM-4-FlashX-250414** 成功 | §2、§3 |
| 真实 Embedding 链路 | ✅ 调用 **Embedding-2**，1024 维向量 | §2 |
| 全链路 6 节点真调 | ✅ 主案例 **5/5 节点 `SUCCESS`**，零降级 | §3.1 |
| 三档风险分级区分度 | ✅ 全量 200 家：**RED 11 / YELLOW 14 / GREEN 175** | §4.1、§5.1 |
| 条件路由差异 | ✅ RED 自动切 `HIGH_RISK_DEEP_DIVE`，其余 `STANDARD` | §4.2 |
| 报告双格式一致性 | ✅ Markdown 10 章 = HTML 10 章 | §3.5 |
| 处置门禁（确定性） | ✅ 4 类处置态全部实测（含证据不足/重新申报） | §4.3 |
| 批量 + 横向对比 + 角色裁剪 | ✅ 200 家批跑 + 名单 + 对比 + 裁剪全通过 | §5 |
| API 全端点 | ✅ 14 个端点/场景实测，含 404/400/422 错误码 | §6 |
| 回归测试 | ✅ **174 / 174 通过** | §7 |

**一句话**：智能体已可交付，全链路真实可用，边界（不预测、不自动处置、不编造事件）守住。

---

## 二、演示环境与双模型链路体检

来源：`_demo_stage1.json`

| 项目 | 值 |
| --- | --- |
| LLM 提供方 | `openai-compatible` |
| LLM 模型 | **GLM-4-FlashX-250414** |
| 统一通道 | `https://open.bigmodel.cn/api/paas/v4` |
| Embedding 模型 | **Embedding-2** |
| Embedding 维度 | **1024** |
| 凭证 | `691827...P3H2`（LLM 与 Embedding 复用同一通道） |
| 统计窗口 | `week / 12`（近 12 周） |
| RAG Top-K | 3 |

**连通性自证（真实网络往返）**：

| 探测 | 状态 | 耗时 | 实证 |
| --- | --- | --- | --- |
| LLM 对话 | `SUCCESS` | 3.29s | 返回 `{"ok":true}` |
| Embedding | `SUCCESS` | 0.27s | 返回 1 条 1024 维向量 |
| RAG 索引重建 | `SUCCESS` | 0.63s | `vector_source=embedding`（真实语义向量，非哈希降级） |

节点级开关（全部开启，即全部走真模型）：
`risk_identification` / `association` / `evidence` / `consistency_check` / `dimension_mapping` = **true**

---

## 三、演示一：主案例真实全链路（供应商 `S-NOR001`，第 52 周）

来源：`_demo_stage2.json`　｜　**总耗时 51.68s**（5 次真实模型往返）　｜　路由 `STANDARD`

### 3.1 逐节点模型调用状态（真调证据）

| 节点 | 状态 | `audit_trace` 实证 |
| --- | --- | --- |
| 维度归类 | `SUCCESS` | `mapped=1; hit_dimensions=1; no_data=5; llm=SUCCESS` |
| 风险识别 | `SUCCESS` | `risk_level=GREEN; visible_events=1; llm=SUCCESS` |
| 关联分析 | `SUCCESS` | `trend=FALLING; cross_dimension=False; dims=1; llm=SUCCESS` |
| 证据转述 | `SUCCESS` | `verified=1; dimensions=1; llm=SUCCESS` |
| 决策建议 | `SUCCESS` | `grounded_llm=GLM-4-FlashX-250414` |
| 一致性校验 | `SUCCESS` | `consistent=True; evidence_sufficient=True; conflicts=0` |

`llm_node_status` 汇总：`decision / risk_identification / association_analysis / evidence / consistency_check` **全部 `SUCCESS`**（5/5，零 `FALLBACK`）。

### 3.2 模型真实产出（非模板）

**风险总结**（风险识别节点）：
> 供应商S-NOR001近12周内共1条风险事件，主要涉及经营风险（核心人员离职），风险等级为低风险（等级「低风险」，分值0.51），无其他维度风险事件。

**趋势描述**（关联分析节点）：
> 加权严重度从上周的2.0下降至本周的1.0，呈现下降趋势。经营风险维度在本窗口期出现1次事件，而知识产权维度在上周有1次事件，本周无事件。

> 趋势 `FALLING`，全文措辞统一用「下降」，**无「下降…但事件数量增加」式自相矛盾**——提示词约束生效。

**证据转述**（证据节点）：
> 第49周，经营风险维度下发生核心人员离职事件，严重程度为一般，来源于人力台账。

**处置建议**（决策节点）：
> 建议对供应商S-NOR001进行观察，并持续监控其经营风险。
> 优先级 `LOW`　｜　执行状态 `PENDING_HUMAN_REVIEW`

### 3.3 规则引擎确定性产出（与 LLM 严格分离）

| 字段 | 值 |
| --- | --- |
| 风险等级 | `GREEN`（低风险） |
| 规则得分 | `0.51`（分值标准 `[0, 0.8)`） |
| 统计窗口 | 近 12 周 |
| 供应商重要性 | `一般`（来源 `BANK_LIST` 银行名单） |
| 报告周期 | 月报 |
| 六维度 | 经营风险 **1** 条（0.51）；其余 5 维 `NO_DATA` 计 0 分 |

> 五维 `NO_DATA` 是**正确行为**：本地数据无司法/失信/知识产权来源，系统**不编造**事件（符合「不编造事件」边界，见 §8.2）。

### 3.4 RAG 政策依据（真实语义检索自证）

```
chunk_id      : 5741e17f1aa5bb49-0000
document_name : mock_policy.md
vector_source : embedding     ← 真实语义向量，非哈希降级
excerpt       : 当证据校验失败时，不得形成正式处置建议……中高风险候选建议应进入人工确认环节
```

### 3.5 报告渲染一致性（历史 bug 复验）

| 验证点 | 结果 |
| --- | --- |
| 趋势正文是否被丢弃 | `rendered_in_md=true`，`rendered_in_html=true` |
| Markdown / HTML 章节数 | `md=10, html=10`，**完全一致** |
| 字符量 | Markdown 1877 / HTML 4533 |

十章逐一对齐：一、供应商基本信息 → 二、当前风险等级 → 三、六维度风险分布 → 四、风险趋势 → 五、关键风险因素 → 六、风险证据 → 七、政策依据 → 八、AI 分析结论 → 九、AI 处置建议 → 十、处置提示与人工审核。

### 3.6 合规边界守门

```
disposition_state : AWAITING_SUPPLIER_REDECLARE
can_redeclare     : true
delivery_audience : [LEADERSHIP, MANAGER]
reason            : 风险等级GREEN、得分0.51且无红线与高严重度事件，问题较小
```

所有 `candidate_actions.execution_status` 恒为 `PENDING_HUMAN_REVIEW`——**模型不产生自动处置**。

### 3.7 产物落盘

- `_demo_report/risk_report_S-NOR001.md`（1877 字符）
- `_demo_report/risk_report_S-NOR001.html`（4533 字符）

---

## 四、演示二：多场景差异化（RED / YELLOW / 证据不足）

来源：`_demo_stage3.json`　（四案例全部真实 LLM 调用）

### 4.1 四案例总览

| 案例 | 供应商 | 风险等级 | 得分 | 路由 | 命中规则 | 处置态 | 是否可重新申报 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RED-失信 | `S-NOR014` | **RED 高风险** | 2.52 | **HIGH_RISK_DEEP_DIVE** | `RL-SEV-3` + `R-DSH-01` | `PENDING_HUMAN_REVIEW` | ❌ |
| RED-司法 | `S-NOR096` | **RED 高风险** | 2.34 | **HIGH_RISK_DEEP_DIVE** | `RL-SEV-3` + `R-JUD-01` | `PENDING_HUMAN_REVIEW` | ❌ |
| YELLOW | `S-NOR033` | **YELLOW 中风险** | 1.45 | `STANDARD` | 无（未触红线） | `PENDING_HUMAN_REVIEW` | ❌ |
| 证据不足 | `S-NOR012` | GREEN 低风险 | 0.00 | `STANDARD` | 无 | **`EVIDENCE_INSUFFICIENT`** | ❌ |

### 4.2 关键差异一：条件路由自动分叉

```
RED 案例   →  analysis_route = HIGH_RISK_DEEP_DIVE   （深度专项链路）
YELLOW/GREEN →  analysis_route = STANDARD             （标准链路）
```

RED 案例的 `hit_rules` 明确给出**红线命中**：
```
RL-SEV-3  category=*      severity=4   统计窗口内出现严重程度>=3的极高风险事件
R-DSH-01  category=失信   severity=4   统计窗口内出现失信类风险事件
```

`grade_basis` 实录（`S-NOR014`）：
> 近12周内共1条风险事件，覆盖1个维度；规则得分2.52，等级「高风险」（分值标准：[2.0, +∞)）；供应商重要性「一般」（系数1，来源：银行名单）；**命中红线规则，直接判最高档**

### 4.3 关键差异二：处置门禁四种分支全覆盖

| 分支 | 触发条件 | 实测案例 | 门禁输出 |
| --- | --- | --- | --- |
| 可重新申报 | 中低档 + 得分≤0.8 + 无红线 + 最高严重度<3 | `S-NOR001`（0.51） | `AWAITING_SUPPLIER_REDECLARE`, `can_redeclare=true` |
| 转人工复核 | 得分超上限 | `S-NOR033`（1.45） | `PENDING_HUMAN_REVIEW`，`reason=规则得分1.45超过可重新申报上限0.8` |
| 转人工复核 | 风险等级为 RED | `S-NOR014`（RED） | `PENDING_HUMAN_REVIEW`，`reason=风险等级为RED，不属于「问题较小」` |
| 证据不足 | 窗口内无事件 | `S-NOR012`（0 条） | `EVIDENCE_INSUFFICIENT`，`reason=证据链不完整，需人工补充材料后复核，不进入供应商重新申报流程` |

> 该门禁**纯确定性、模型不参与**——避免 LLM 放宽门禁，是合规关键设计。

### 4.4 关键差异三：趋势研判与一致性校验

| 案例 | 趋势类型 | 趋势描述（模型产出） | 一致性校验 |
| --- | --- | --- | --- |
| `S-NOR014` | `SUDDEN_JUMP` | 加权严重度突增，从 0.0 急剧上升到 4.0 | `consistent=true`，`conflicts=[]` |
| `S-NOR096` | `RISING` | 严重度由 3.0 上升至 4.0，司法维度由 0 增至 1 | `consistent=true` |
| `S-NOR033` | `RISING` | 严重度由 1.0 上升至 4.0 | `consistent=true` |
| `S-NOR012` | `STEADY` | 过去 12 周持平无变化 | `null`（无证据，**不强行生成结论**） |

> `S-NOR012` 证据不足时一致性校验置空、`recommendation` 为空、无候选动作——**系统在无证据时保持沉默**，不编造建议。

---

## 五、演示三：批量分析 + 重点监测名单 + 横向对比 + 角色裁剪

来源：`_demo_stage4.json`

### 5.1 全量批量（200 家，第 52 周）

| 指标 | 值 |
| --- | --- |
| 分析总数 | **200** |
| 失败数 | **0** |
| 等级分布 | **RED 11 / YELLOW 14 / GREEN 175** |
| 报告周期分布 | 周报 128 / 月报 72 |
| 待重新申报名单 | 32 家 |
| 未知供应商 | 0 |

### 5.2 重点监测名单（Top 10，按等级+分值排序）

| 排名 | 供应商 | 等级 | 分值 | 重要性 | 周期 | 趋势 | 关键维度 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `S-NOR062` | RED | 2.856 | 重要 | 周报 | SUDDEN_JUMP | 失信 |
| 2 | `S-NOR014` | RED | 2.520 | 一般 | 月报 | SUDDEN_JUMP | 失信 |
| 3 | `S-NOR096` | RED | 2.340 | 一般 | 月报 | RISING | 司法 |
| 4 | `S-NOR088` | RED | 1.560 | 一般 | 月报 | SUDDEN_JUMP | 司法 |
| 5 | `S-SUD171` | RED | 1.440 | 重要 | 周报 | RISING | 经营状况 |
| 6 | `S-SUD166` | RED | 1.360 | 一般 | 月报 | SUDDEN_JUMP | 经营状况 |
| 7 | `S-ACC134` | RED | 1.296 | 重要 | 周报 | SUDDEN_JUMP | 知识产权 |
| 8 | `S-NOR022` | RED | 1.296 | 重要 | 周报 | SUDDEN_JUMP | 知识产权 |
| 9 | `S-REC183` | RED | 1.248 | 重要 | 周报 | SUDDEN_JUMP | 司法 |
| 10 | `S-SUD161` | RED | 1.224 | 重要 | 周报 | SUDDEN_JUMP | 经营状况 |

> 名单同时给出「风险等级 + 供应商重要性 + 报告周期 + 趋势 + 关键维度」，可直接用于人工研判。

### 5.3 横向对比数据块（纯计算，无模型调用）

```
supplier_count      : 200
level_distribution  : RED 11 / YELLOW 14 / GREEN 175
median_score        : 0.0
维度基线            : 六维度中位数均为 0.0（大部分供应商无事件，中位数落在 0）
note                : 本批共200家供应商；分值中位数0.00；横向对比数据仅供人工研判，不参与任何自动处置
```

### 5.4 角色下发裁剪（两层受众）

| 角色 | 横向对比 | 实测 |
| --- | --- | --- |
| `LEADERSHIP`（高级管理） | ✅ 下发 | `comparison_is_null=false`，`median_score=1.985` |
| `MANAGER`（普通管理） | ❌ 不下发 | `comparison_is_null=true`，附 `cross_supplier_comparison_withheld` 说明 |

### 5.5 异常入参容错

```
输入  : ["S-NOR001", "NOT-EXIST-999"]
结果  : analyzed_count=1, unknown_suppliers=["NOT-EXIST-999"], failed_count=0
```
> 未知供应商被登记而非抛错，**不阻断整批**。

---

## 六、演示四：API 层全端点实测（真实 ASGI 往返）

来源：`_demo_stage4.json::api`

| # | 端点 / 场景 | HTTP | 业务码 | 结果 |
| --- | --- | --- | --- | --- |
| 1 | `GET /health` | 200 | 0 | ✅ 含 `trace_id` |
| 2 | `POST /agent/analyze`（单供应商） | 200 | 0 | ✅ `mode=single`，`risk_level=RED` |
| 3 | `POST /agent/analyze`（批量同步） | 200 | 0 | ✅ 3 家，`RED 1/YELLOW 1/GREEN 1`，`reports` 按需裁剪 |
| 4 | `POST /agent/analyze`（`audience=MANAGER`） | 200 | 0 | ✅ 横向对比被扣留 |
| 5 | `POST /agent/analyze`（`audience=LEADERSHIP`） | 200 | 0 | ✅ 横向对比正常下发 |
| 6 | `POST /agent/analyze`（`async=true`） | 200 | 0 | ✅ 立即返回 `task_id` + `polling_url` |
| 7 | `GET /agent/task/{task_id}` | 200 | 0 | ✅ 返回任务状态 |
| 8 | `GET /agent/task/{不存在}` | **404** | 40000… | ✅ `unknown task_id` |
| 9 | `GET /agent/report/{supplier_id}` | 200 | 0 | ✅ 返回结构化报告 |
| 10 | `GET /agent/report/{不存在}` | **404** | 40400 | ✅ `unknown supplier_id` |
| 11 | `GET /agent/tools/supplier/{id}/evidence` | 200 | 0 | ✅ 返回证据取数结果 |
| 12 | `GET /agent/tools/supplier/{id}/history` | 200 | 0 | ✅ 返回分级+趋势+维度 |
| 13 | `POST /agent/review`（APPROVED） | 200 | 0 | ✅ 回写成功，`review_result=APPROVED` |
| 14 | `POST /agent/review`（重复 run_id） | **400** | 40000 | ✅ `Review already exists for run_id=...` |
| 15 | `POST /agent/review`（非法枚举） | **422** | 42200 | ✅ 参数校验失败 |
| 16 | `POST /agent/redeclare` | 200 | 0 | ✅ 登记 `AWAITING_SUPPLIER_REDECLARE`，保留附件引用 |
| 17 | `GET /agent/reviews`（按处置态过滤） | 200 | 0 | ✅ `total=1` |

**统一响应信封**（所有接口含错误均一致）：
```json
{ "code": 0, "message": "ok", "data": { ... }, "trace_id": "3f2c..." }
```

---

## 七、演示五：全量回归测试

```
$ python -m pytest -q
........................................................................ [ 41%]
........................................................................ [ 82%]
..............................                                           [100%]
```

| 指标 | 值 |
| --- | --- |
| 测试用例总数 | **174** |
| 通过 | **174** |
| 失败 / 错误 | **0 / 0** |
| 通过率 | **100%** |

按模块分布：

| 测试文件 | 用例数 | 覆盖点 |
| --- | --- | --- |
| `test_api_delivery.py` | 21 | API 交付层 / 信封 / 鉴权 / 角色裁剪 |
| `test_rules_engine.py` | 19 | 分级引擎 |
| `test_decision_contract.py` | 14 | 决策契约 |
| `test_disposition.py` | 12 | 处置门禁 |
| `test_grade_tiers.py` | 12 | 档位表 |
| `test_window.py` | 11 | 统计窗口 |
| `test_workflow.py` | 11 | 工作流图 |
| `test_agents_llm_text.py` | 10 | LLM 文案节点 |
| `test_report_export.py` | 10 | 报告导出 |
| `test_importance.py` | 9 | 供应商重要性 |
| `test_batch_comparison.py` | 9 | 横向对比 |
| `test_dimension_mapping.py` | 7 | 维度归类 |
| `test_batch_service.py` | 6 | 批量服务 |
| `test_llm.py` | 6 | LLM 工厂/降级 |
| `test_consistency_check.py` | 5 | 一致性校验 |
| `test_rag.py` | 4 | 向量检索 |
| `test_policy_retrieval.py` | 3 | 政策检索 |
| `test_decision_grounding.py` | 3 | 决策落地 |
| `test_review_feedback.py` | 2 | 审核回写 |

---

## 八、交接清单

### 8.1 核心能力交付状态

| 能力 | 状态 | 说明 |
| --- | --- | --- |
| 单供应商全链路分析 | ✅ 完成 | 6 节点协同，真实 LLM |
| 三档风险分级 | ✅ 完成 | 规则可配置（换规则不改代码） |
| 条件路由 | ✅ 完成 | RED → 深度专项；其余 → 标准 |
| 报告双格式导出 | ✅ 完成 | Markdown / HTML，10 章一致 |
| RAG 政策依据 | ✅ 完成 | 真实 embedding 语义检索 |
| 一致性校验 | ✅ 完成 | 模型级交叉复核 |
| 处置门禁 | ✅ 完成 | 纯确定性，4 分支全覆盖 |
| 批量 + 名单 + 对比 | ✅ 完成 | 200 家 / 0 失败 |
| 角色下发裁剪 | ✅ 完成 | LEADERSHIP / MANAGER 两层 |
| API 交付层 | ✅ 完成 | 信封 + 鉴权 + 异步任务 + 错误码 |
| 人工审核回写 | ✅ 完成 | 去重校验 + 处置态过滤 |
| 供应商重新申报 | ✅ 完成 | 登记式受理（预留扩展） |

### 8.2 明确边界（不可越界，需求已锁定）

1. **不做预测**——仅输出「当前风险」，不输出未来风险预测。
2. **不自动处置**——所有处置建议恒为 `PENDING_HUMAN_REVIEW`，由人工确认。
3. **不编造事件**——无来源的维度标 `NO_DATA` 计 0 分，不虚构风险。
4. **门禁纯确定性**——是否允许重新申报由规则判定，模型不参与。

### 8.3 后端接手入口

| 操作 | 入口 |
| --- | --- |
| 单供应商分析 | `from app.service import analyze_supplier` |
| 批量分析 | `from app.batch.service import analyze_batch` |
| 启动 HTTP 服务 | `python -m app.main serve` |
| 导出接口契约 | `python -m app.main export-contracts` |
| 替换分级规则 | 只改 `app/rules/rules.yaml`，无需改代码 |
| 调整统计窗口 | 请求传 `window_unit` / `window_size` |
| 关闭指定 LLM 节点 | `.env` 修改 `LLM_TEXT_*` 开关（自动降级为确定性模板） |

### 8.4 产物文件索引

| 文件 | 内容 |
| --- | --- |
| `交付演示报告.md` | 本报告 |
| `交付演示报告.html` | 本报告 HTML 版 |
| `_demo_report/risk_report_S-NOR001.md/.html` | GREEN 案例报告 |
| `_demo_report/risk_report_S-NOR014.md/.html` | RED 案例报告（失信） |
| `_demo_report/risk_report_S-NOR096.md/.html` | RED 案例报告（司法） |
| `_demo_report/risk_report_S-NOR033.md/.html` | YELLOW 案例报告 |
| `_demo_report/risk_report_S-NOR012.md/.html` | 证据不足案例报告 |

---

## 九、结论

本次演示以**真实模型调用**为唯一依据，覆盖了从「单供应商深度分析」到「200 家批量聚合」再到「API 交付层」的完整链路：

- **模型链路真实**：LLM 与 Embedding 双通道均实测连通，6 节点零降级；
- **分级有区分度**：三档风险在真实数据上均产生样本，红线规则与档位表生效；
- **合规边界稳固**：不预测、不自动处置、不编造事件、门禁确定性；
- **接口可交付**：17 个 API 场景全通过，错误码语义与 HTTP 状态码一致；
- **回归无退化**：174 个用例 100% 通过。

**智能体已具备交接条件。**

---

*报告生成：2026-09-11　｜　演示脚本：`_demo_stage1~4.py`（已清理）　｜　原始 JSON 证据：`_demo_stage1~4.json`*
